# 数据库优化

> 基于你的项目经验（MySQL、PostgreSQL + Pgvector）整理的数据库知识

---

## 📓 学习目标

- 掌握MySQL索引优化
- 理解MySQL锁机制与事务
- 熟练使用Explain分析慢查询
- 理解MVCC原理
- 掌握PostgreSQL基础与高级特性
- 理解Pgvector向量检索
- 掌握数据库连接池配置

---

## 1. MySQL索引优化

### 1.1 索引原理

**B+树结构**

```
        [10, 20]
       /     \
    [5,8]   [15,18]  [25,30]
   / | \    /  | \    /  |  \
[1][5][8][15][18][20][25][30][40]
```

**B+树特点**

- 所有数据在叶子节点
- 叶子节点通过链表连接（范围查询快）
- 高度低（3-4层）
- 节点存储多个键值

### 1.2 索引类型

**聚簇索引**

- 主键索引
- 数据和索引在一起
- 一个表只能有一个

```sql
CREATE TABLE `user` (
  `id` BIGINT PRIMARY KEY,
  `name` VARCHAR(50),
  `age` INT
) ENGINE=InnoDB;
```

**非聚簇索引（辅助索引）**

- 普通索引、唯一索引
- 叶子节点存储主键值（回表查询）

```sql
CREATE INDEX idx_name ON `user`(`name`);
CREATE UNIQUE INDEX idx_age ON `user`(`age`);
```

**联合索引**

- 最左前缀原则

```sql
CREATE INDEX idx_name_age ON `user`(`name`, `age`);
```

### 1.3 索引优化策略

**1. 选择性高的列建索引**

```sql
SELECT COUNT(DISTINCT `name`) / COUNT(*) FROM `user`;
```

**2. 遵循最左前缀原则**

```sql
CREATE INDEX idx_a_b_c ON `table`(`a`, `b`, `c`);

WHERE a = 1          -- 使用索引
WHERE a = 1 AND b = 2  -- 使用索引
WHERE b = 2          -- 不使用索引
```

**3. 覆盖索引**

```sql
CREATE INDEX idx_name_age_email ON `user`(`name`, `age`, `email`);

SELECT `name`, `age` FROM `user` WHERE `name` = '张三';
```

**4. 避免索引失效**

- 在索引列上做函数计算
- 隐式类型转换
- LIKE '%xxx'
- OR条件
- !=、<>、NOT IN

### 1.4 项目中的索引优化

**ERP系统：设备查询优化**

```sql
-- 优化前（全表扫描）
SELECT * FROM `device` 
WHERE `user_id` = 123 AND `status` = 1;

-- 优化后（添加联合索引）
CREATE INDEX idx_user_status ON `device`(`user_id`, `status`);
```

**智能合同：合同审批流程**

```sql
-- 优化前
SELECT * FROM `contract` 
WHERE `status` = 'APPROVING' 
ORDER BY `create_time` DESC;

-- 优化后
CREATE INDEX idx_status_time ON `contract`(`status`, `create_time` DESC);
```

---

## 2. MySQL事务

### 2.1 ACID特性

| 特性 | 说明 |
|------|------|
| 原子性（A） | 事务不可分割 |
| 一致性（C） | 事务前后数据一致 |
| 隔离性（I） | 事务间互不影响 |
| 持久性（D） | 事务提交后永久生效 |

### 2.2 隔离级别

| 隔离级别 | 脏读 | 不可重复读 | 幻读 |
|----------|------|-----------|------|
| READ_UNCOMMITTED | 是 | 是 | 是 |
| READ_COMMITTED | 否 | 是 | 是 |
| REPEATABLE_READ（默认）| 否 | 否 | 是 |
| SERIALIZABLE | 否 | 否 | 否 |

```sql
SET SESSION TRANSACTION ISOLATION LEVEL READ_COMMITTED;
SET GLOBAL TRANSACTION ISOLATION LEVEL REPEATABLE_READ;
```

### 2.3 MVCC（多版本并发控制）

**原理**：通过undo log实现多版本

```
事务A读取 → undo log → 数据快照
事务B修改 → 写新数据 + undo log
事务A读取 → undo log → 旧数据
```

**Read View**

- m_ids：活跃事务ID列表
- min_trx_id：最小活跃事务ID
- max_trx_id：最大活跃事务ID

### 2.4 项目中的事务应用

**智能合同：合同审批**

```java
@Transactional(rollbackFor = Exception.class)
public void approveContract(Long contractId, Long approverId) {
    Contract contract = contractMapper.selectById(contractId);
    contract.setStatus(Status.APPROVED);
    contract.setApprover(approverId);
    contractMapper.updateById(contract);
    
    Approval approval = new Approval();
    approval.setContractId(contractId);
    approval.setApprover(approverId);
    approvalMapper.insert(approval);
    
    Record record = new Record();
    record.setContractId(contractId);
    record.setOperation("APPROVE");
    recordMapper.insert(record);
}
```

---

## 3. MySQL锁机制

### 3.1 锁类型

**全局锁**

```sql
FLUSH TABLES WITH READ LOCK;
UNLOCK TABLES;
```

**表级锁**

```sql
LOCK TABLES `user` READ;
LOCK TABLES `user` WRITE;
UNLOCK TABLES;
```

**行级锁**

- 共享锁（S锁）
- 排他锁（X锁）

```sql
SELECT * FROM `user` WHERE `id` = 1 FOR UPDATE;
SELECT * FROM `user` WHERE `id` = 1 LOCK IN SHARE MODE;
```

### 3.2 间隙锁

**作用**：防止幻读

```sql
SELECT * FROM `user` WHERE `id` BETWEEN 5 AND 10 FOR UPDATE;
-- 锁定 (5, 10) 之间的间隙
```

### 3.3 死锁

**死锁条件**

1. 互斥条件
2. 请求与保持
3. 不剥夺条件
4. 循环等待

**查看死锁**

```sql
SHOW ENGINE INNODB STATUS;

-- 查看当前事务
SELECT * FROM information_schema.INNODB_TRX;

-- 查看锁
SELECT * FROM information_schema.INNODB_LOCKS;
```

---

## 4. Explain分析

### 4.1 Explain字段

| 字段 | 说明 |
|------|------|
| id | 查询序列号 |
| select_type | 查询类型 |
| table | 访表 |
| type | 访问类型 |
| possible_keys | 可能用到的索引 |
| key | 实际使用的索引 |
| key_len | 索引长度 |
| ref | 索引比较列 |
| rows | 预估扫描行数 |
| Extra | 额外信息 |

### 4.2 type字段（访问类型）

| 类型 | 性能 | 说明 |
|------|------|------|
| system | 最好 | 表只有一行 |
| const | 很好 | 主键或唯一索引等值查询 |
| eq_ref | 好 | 联表查询，主键或唯一索引 |
| ref | 较好 | 普通索引等值查询 |
| range | 一般 | 范围查询 |
| index | 较差 | 索引扫描 |
| ALL | 最差 | 全表扫描 |

### 4.3 项目中的慢查询优化

**ERP系统：设备维修查询**

```sql
-- 优化前（type: ALL，rows: 10000+）
EXPLAIN SELECT * FROM `device_repair` 
WHERE `device_id` IN (
    SELECT `id` FROM `device` WHERE `user_id` = 123
);

-- 优化后（type: ref，rows: 10）
CREATE INDEX idx_user ON `device`(`user_id`);
CREATE INDEX idx_device ON `device_repair`(`device_id`);

EXPLAIN SELECT dr.* FROM `device_repair` dr
JOIN `device` d ON dr.device_id = d.id
WHERE d.user_id = 123;
```

---

## 5. PostgreSQL基础

### 5.1 与MySQL对比

| 特性 | MySQL | PostgreSQL |
|------|-------|------------|
| 复杂查询 | 一般 | 优秀 |
| 事务支持 | 优秀 | 优秀 |
| JSON支持 | 较弱 | 优秀 |
| 并发性能 | 优秀 | 优秀 |
| 扩展性 | 一般 | 优秀 |
| 生态 | 丰富 | 丰富 |

### 5.2 JSON支持

```sql
CREATE TABLE `document` (
  `id` SERIAL PRIMARY KEY,
  `content` JSONB
);

INSERT INTO `document` (`content`) 
VALUES ('{"title": "Test", "tags": ["tag1", "tag2"]}');

SELECT `content`->>'title' FROM `document` 
WHERE `content`->'tags' ? 'tag1';
```

### 5.3 全文检索

```sql
CREATE TABLE `article` (
  `id` SERIAL PRIMARY KEY,
  `title` TEXT,
  `content` TEXT
);

CREATE INDEX idx_title ON `article` USING GIN(to_tsvector('english', `title`));
CREATE INDEX idx_content ON `article` USING GIN(to_tsvector('english', `content`));

SELECT * FROM `article` 
WHERE to_tsvector('english', `content`) @@ to_tsquery('search');
```

---

## 6. Pgvector向量检索

### 6.1 安装与启用

```sql
CREATE EXTENSION vector;
```

### 6.2 创建向量表

```sql
CREATE TABLE `document` (
  `id` SERIAL PRIMARY KEY,
  `title` TEXT,
  `content` TEXT,
  `embedding` vector(1536)
);
```

### 6.3 插入向量数据

```sql
INSERT INTO `document` (`title`, `content`, `embedding`) 
VALUES (
  'Java编程',
  'Java是一门面向对象编程语言',
  '[0.1, 0.2, 0.3, ...]'
);
```

### 6.4 创建向量索引

```sql
CREATE INDEX idx_embedding ON `document` 
USING ivfflat (`embedding` vector_cosine_ops) 
WITH (lists = 100);
```

### 6.5 向量相似度查询

```sql
SELECT `title`, `content`, 
       1 - (`embedding` <=> '[0.1, 0.2, 0.3, ...]'::vector) AS similarity
FROM `document`
ORDER BY `embedding` <=> '[0.1, 0.2, 0.3, ...]'::vector
LIMIT 10;
```

### 6.6 项目中的向量检索

**电子技术标准化：相关性检索**

```java
@Service
public class DocumentSearchService {
    
    @Autowired
    private JdbcTemplate jdbcTemplate;
    
    @Autowired
    private EmbeddingService embeddingService;
    
    public List<Document> searchByKeyword(String keyword, int limit) {
        float[] embedding = embeddingService.generate(keyword);
        
        String sql = "SELECT id, title, content, " +
                      "1 - (embedding <=> ?) as similarity " +
                      "FROM document " +
                      "ORDER BY embedding <=> ? " +
                      "LIMIT ?";
        
        String vectorStr = arrayToVectorString(embedding);
        
        return jdbcTemplate.query(sql, new Object[]{vectorStr, vectorStr, limit},
            (rs, rowNum) -> {
                Document doc = new Document();
                doc.setId(rs.getLong("id"));
                doc.setTitle(rs.getString("title"));
                doc.setContent(rs.getString("content"));
                doc.setSimilarity(rs.getDouble("similarity"));
                return doc;
            });
    }
    
    private String arrayToVectorString(float[] array) {
        StringBuilder sb = new StringBuilder("[");
        for (int i = 0; i < array.length; i++) {
            if (i > 0) sb.append(", ");
            sb.append(array[i]);
        }
        sb.append("]");
        return sb.toString();
    }
}
```

---

## 7. 数据库连接池

### 7.1 HikariCP配置

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/erp?useUnicode=true&characterEncoding=utf8
    username: root
    password: 123456
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: SELECT 1
```

### 7.2 Druid配置

```yaml
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
```

### 7.3 连接池参数优化

| 参数 | 说明 | 推荐值 |
|------|------|--------|
| initialSize | 初始连接数 | 5-10 |
| minIdle | 最小空闲连接数 | 5-10 |
| maxActive | 最大活动连接数 | CPU核数 * 2 + 1 |
| maxWait | 获取连接最大等待时间(ms) | 60000 |
| validationQuery | 连接检测SQL | SELECT 1 |

---

## 8. 项目应用场景

### 8.1 ERP系统：缓存+索引优化

```java
@Service
public class DeviceService {
    
    @Autowired
    private DeviceMapper deviceMapper;
    
    @Autowired
    private RedisTemplate<String, Device> redisTemplate;
    
    public Device getDevice(Long deviceId) {
        String key = "device:" + deviceId;
        Device device = redisTemplate.opsForValue().get(key);
        
        if (device == null) {
            device = deviceMapper.selectById(deviceId);
            redisTemplate.opsForTemplate().set(key, device, 3600, TimeUnit.SECONDS);
        }
        
        return device;
    }
    
    public List<Device> getUserDevices(Long userId, String status) {
        String key = "user:devices:" + userId + ":" + status;
        List<Device> devices = redisTemplate.opsForValue().get(key);
        
        if (devices == null) {
            devices = deviceMapper.selectByUserAndStatus(userId, status);
            redisTemplate.opsForValue().set(key, devices, 300, TimeUnit.SECONDS);
        }
        
        return devices;
    }
}
```

### 8.2 万物云对接：数据同步

```java
@Service
public class ContractSyncService {
    
    @Autowired
    private ContractMapper contractMapper;
    
    @Autowired
    private WanyunApiClient wanyunClient;
    
    @Transactional(rollbackFor = Exception.class)
    public void syncContract(Long contractId) {
        Contract contract = contractMapper.selectById(contractId);
        
        if (contract.getSyncStatus() == SyncStatus.SYNCED) {
            return;
        }
        
        wanyunClient.syncContract(contract);
        
        contract.setSyncStatus(SyncStatus.SYNCED);
        contract.setSyncTime(new Date());
        contractMapper.updateById(contract);
    }
}
```

### 8.3 文档处理：批量插入优化

```java
@Service
public class DocumentProcessService {
    
    @Autowired
    private DocumentMapper documentMapper;
    
    @Transactional
public void batchInsert(List<Document> documents) {
        if (CollectionUtils.isEmpty(documents)) {
            return;
        }
        
        int batchSize = 1000;
        for (int i = 0; i < documents.size(); i += batchSize) {
            int end = Math.min(i + batchSize, documents.size());
            List<Document> batch = documents.subList(i, end);
            documentMapper.batchInsert(batch);
        }
    }
}
```

---

## 面试高频题

### 基础题

1. MySQL索引类型有哪些？
2. B+树的特点是什么？
3. 索引失效的场景有哪些？
4. 事务的ACID特性？
5. MySQL的隔离级别有哪些？
6. 什么是MVCC？
7. Explain的type字段有哪些？
8. 如何优化慢查询？

### 中等题

1. 聚簇索引和非聚簇索引的区别？
2. 联合索引的最左前缀原则？
3. 覆盖索引是什么？
4. MySQL锁的类型有哪些？
5. 什么是间隙锁？
6. 如何查看和解决死锁？
7. MySQL和PostgreSQL的对比？
8. 如何配置数据库连接池？

### 高级题

1. B+树的插入和删除过程？
2. MVCC的实现原理？
3. 如何实现数据库读写分离？
4. 手写一个简单的数据库连接池？
5. Pgvector的实现原理？
6. 如何实现向量相似度搜索？
7. MySQL事务的实现原理？

---

## 学习建议

1. **重点掌握**：索引优化、事务、Explain分析
2. **理解原理**：B+树、MVCC、锁机制
3. **实践应用**：项目中的性能优化案例
4. **准备代码**：手写连接池、批量插入

**参考文档**：
- MySQL官方文档
- PostgreSQL官方文档
- 高性能MySQL
- 数据库系统实现

---

**更新进度到学习进度.md**